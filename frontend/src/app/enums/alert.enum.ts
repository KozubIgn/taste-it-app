export enum AlertType {
  INFO = 'info',
  SUCCESS = 'success',
  ERROR = 'danger',
  WARNING = 'warning',
  PRIMARY = 'primary',
}

export enum AlertDirection {
  BOTTOM = 'bottom'
}

export enum AlertAlign{
  RIGHT = 'right'
}